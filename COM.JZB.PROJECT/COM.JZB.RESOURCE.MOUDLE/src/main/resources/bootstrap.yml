spring:
  cloud:
    config:
      label: 'master'
      uri: http://localhost:8506
      name: 'jzb-resource'
      profile: 'test'
      status: 1
      backup:
        enable: true
        fallbackLocation: ./config/application-release.yml
       