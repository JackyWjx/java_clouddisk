spring:
  cloud:
    config:
      label: 'master'
      uri: http://localhost:7506
      name: 'jzb-message'
      profile: 'test'
      status: 1
      backup:
        enable: true
        fallbackLocation: ./config/application-release.yml